{
  "name": "@celebratebanner/image-intelligence",
  "version": "0.1.0",
  "description": "Pure image-intelligence decisions for the WOW render path: non-destructive orientation correction, hero framing that eliminates letterbox dead zones, near-duplicate photo curation, and banner-text placeholder cleanup. No DOM, no pixels, deterministic.",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": { ".": "./src/index.ts" },
  "scripts": { "test": "node --test 'test/*.test.ts'", "typecheck": "tsc --noEmit" },
  "devDependencies": { "@types/node": "^22.10.0", "typescript": "^5.7.2" },
  "files": ["src", "README.md"]
}

{
  "name": "@celebratebanner/render-orchestrator",
  "version": "0.1.0",
  "description": "The render orchestrator. Translates the WOW Engine's creative decisions (a WOWPresentation) into a renderer-ready RenderPlan: placement, palette, spacing, layering, export targets, and quality checks. Makes NO creative decisions and generates NO pixels. Deterministic, dependency-free.",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "node --test 'test/*.test.ts'",
    "typecheck": "tsc --noEmit",
    "fixtures": "node test/generate-fixtures.ts"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.2"
  },
  "files": ["src", "README.md"]
}

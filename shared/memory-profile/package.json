{
  "name": "@celebratebanner/memory-profile",
  "version": "0.1.0",
  "description": "AI Creative Director analysis stage. Transforms uploaded photos into a structured Memory Profile — the single source of truth for every renderer. Deterministic, pixel-free, dependency-free.",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "node --test 'test/*.test.ts'",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.2"
  },
  "files": ["src", "README.md"]
}
